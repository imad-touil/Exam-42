#include <stdio.h> 
#include <ctype.h>


int main()
{
	int		a;
	int		b;
	int		c;
	char	ch;

	// int ret = scanf("%d %d %d", &a, &b, &c);
	// printf("return value ==> %d, a ==> %d, b ==>> %d, c ==>> %d\n", ret, a, b, c);
	int	ret = scanf(" ");
	printf("return value ==> '%d', character ==> '%c'\n", ret, c);
}
